# Netlify Configuration for MindCare AI Frontend
[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — redirect all routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
