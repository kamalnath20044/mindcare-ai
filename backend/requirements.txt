# MindCare AI — Production Dependencies (Render)
# v4.0 — Full Mental Health Platform

fastapi==0.110.0
uvicorn==0.29.0
python-dotenv==1.0.1
httpx==0.27.0
python-multipart==0.0.9
PyJWT==2.8.0
numpy==1.26.4
Pillow==10.2.0
requests==2.32.2

# AI/ML — Sentiment Analysis (DistilBERT)
# transformers and torch are optional — fallback is built in

# AI — GPT-3.5 + LangChain
openai>=1.12.0
langchain>=0.2.0
langchain-openai>=0.1.0

# Background Jobs
APScheduler==3.10.4

# Email Alerts
sendgrid>=6.10.0

# Input Sanitization (built-in stdlib — no extra deps needed)

# Voice (optional)
SpeechRecognition==3.10.1
pydub==0.25.1