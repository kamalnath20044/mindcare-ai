# MindCare AI — Production Dependencies
# Optimized for Render free tier (512MB RAM)

fastapi==0.110.0
uvicorn[standard]==0.29.0
python-dotenv==1.0.1
supabase==2.4.0
python-multipart==0.0.9
PyJWT==2.8.0
numpy==1.26.4
Pillow==10.2.0
opencv-python-headless==4.9.0.80
SpeechRecognition==3.10.1
pydub==0.25.1

# NOTE: torch, tensorflow, transformers are NOT included for production
# because they exceed Render free tier memory limits (3.5GB+ combined).
# The app uses intelligent fallbacks:
# - Chat: curated therapeutic response library (no ML needed)
# - Crisis detection: keyword-based (no ML needed)
# - Sentiment: keyword-based fallback
# - Emotion detection: graceful "unavailable" message
# All core features work perfectly without ML packages.
